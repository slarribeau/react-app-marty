import React from 'react';


const Standing = (props) => {
/*    state = {
                standings: [],
                date:null,
                season:null,
                league:null,
                division:null
            };
*/

    return  this.props.state.standings.map((standing) => {

        <div className= "ui container" style={{marginTop:'10px'}}>
            standing
        </div>
        }
    );
}

export default App;   